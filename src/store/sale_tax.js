import __base_store from '@/store/__base_store'

/**
 * Store de impuestos sobre ventas (modelo `sale_tax`) construido desde el factory común.
 */
export default __base_store({
	state: {
		model_name: 'sale_tax',
	},
})
